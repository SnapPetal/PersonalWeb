spring:
  application:
    name: "personal"
  thymeleaf:
    cache: false
  security:
    oauth2:
      client:
        registration:
          auth0:
            client-id: "${AUTH0_CLIENT_ID:KFWch7LS8AzxvQ75TzIBr5zqJlD53s26}"
            client-secret: "${AUTH0_CLIENT_SECRET:MxY6OOrNHpVxaIJoRvwQnvoxEMSdj3jG0zE7iCnpQUVRhvHQZ0QWAJddHfHiOJ4N}"
            authorization-grant-type: client_credentials
            audience: "${AUTH0_AUDIENCE:https://foosball.thonbecker.biz}"
        provider:
          auth0:
            token-uri: "${AUTH0_TOKEN_URL:https://thonbecker.us.auth0.com/oauth/token}"
  cloud:
    openfeign:
      oauth2:
        enabled: true
        clientRegistrationId: auth0

  web:
    resources:
      chain:
        enabled: true
      cache:
        period: 0
        cachecontrol:
          no-cache: true
          no-store: true
          must-revalidate: true

bible:
  verse:
    api:
      url: https://bibleverse.thonbecker.com/
    cache:
      duration: 24

foosball:
  api:
    base-url: https://foosball.thonbecker.biz