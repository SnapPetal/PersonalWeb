services:
  postgres:
    environment:
      - POSTGRES_DB=dbmaster
      - POSTGRES_USER=dbmasteruser
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    image: postgres:18.1
    network_mode: host
